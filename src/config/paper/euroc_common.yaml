%YAML:1.0

# ============================================================================
# EuRoC common stereo-inertial backbone (paper method "B0").
#
# This is the SINGLE base every paper method is generated from. Do not copy this
# file per method: apply an overlay from src/config/paper/overlays/ with
#
#   python3 scripts/generate_paper_config.py \
#     --base src/config/paper/euroc_common.yaml \
#     --overlay src/config/paper/overlays/<method>.yaml \
#     --out <run_dir>/resolved_config.yaml
#
# Every tunable knob is declared here EXPLICITLY, including the ones that are off
# for B0. Relying on C++ defaults hides differences from
# scripts/audit_method_config_diff.py, which is what let the earlier confounded
# ablation through (Sem-GeoDF silently enabled visual_adaptive_quality and
# imu_adaptive_covariance while the baselines did not).
#
# An overlay may only change keys listed for its method in
# src/config/paper/allowed_differences.yaml. The audit fails otherwise.
# ============================================================================

imu: 1
num_of_cam: 2

imu_topic: "/imu0"
image0_topic: "/cam0/image_raw"
image1_topic: "/cam1/image_raw"
output_path: "~/output/"

cam0_calib: "cam0_mei.yaml"
cam1_calib: "cam1_mei.yaml"
image_width: 752
image_height: 480

estimate_extrinsic: 0

body_T_cam0: !!opencv-matrix
   rows: 4
   cols: 4
   dt: d
   data: [0.0148655429818, -0.999880929698, 0.00414029679422, -0.0216401454975,
           0.999557249008, 0.0149672133247, 0.025715529948,  -0.064676986768,
           -0.0257744366974, 0.00375618835797, 0.999660727178, 0.00981073058949,
           0, 0, 0, 1]

body_T_cam1: !!opencv-matrix
   rows: 4
   cols: 4
   dt: d
   data: [0.0125552670891, -0.999755099723, 0.0182237714554, -0.0198435579556,
           0.999598781151, 0.0130119051815, 0.0251588363115, 0.0453689425024,
          -0.0253898008918, 0.0179005838253, 0.999517347078, 0.00786212447038,
          0, 0, 0, 1]

multiple_thread: 1

# ---------------------------------------------------------------- front-end ---
# Shared by every method. Changing any of these in an overlay is a confound.
max_cnt: 150
min_dist: 30
freq: 10
F_threshold: 1.0
show_track: 1
flow_back: 1

# --------------------------------------------------------- semantic expert ---
# Off for B0. Owned by the `semantic` / `union*` / `full_adaptive` overlays.
sem_enable: 0
sem_mask_topic: "/dynamic_mask"
sem_static_value: 255
sem_mask_max_age_ms: 150.0
sem_use_latest_mask: 1
sem_block_on_mask: 0
sem_mask_gated: 0
sem_vote_frames: 2
sem_activate_ratio: 0.015
sem_activate_ema: 0.15
sem_deactivate_frac: 0.6

# ------------------------------------------------------------ gated union ---
# Off for B0. `sem_geodf_fusion` is the union itself; `sem_adaptive_policy` is the
# online three-state gate. Both are owned by the `union*` overlays.
sem_geodf_fusion: 0
sem_adaptive_policy: 0
# Oracle control. MUST stay -1 in every paper config: a non-negative value feeds
# the ground-truth dynamic level into the online estimator. The audit rejects any
# method that sets it otherwise.
sem_policy_dynamic_level: -1

# Policy thresholds selected on VIODE city_day TRAIN ONLY (EuRoC is hold-out; thresholds are never refit here)
# (results/sem_policy_tuning_postfix/selected_params.yaml). They are identical
# for every method that runs the policy, so they can never explain a difference
# between two methods.
sem_policy_burst_ratio: 0.18
sem_policy_strong_ratio: 0.18
sem_policy_overlap_ratio: 0.35
sem_policy_overlap_ema: 0.20
sem_policy_min_geo_candidates: 4
sem_policy_min_sem_candidates: 4
sem_policy_min_intersection: 2
sem_policy_overlap_support_saturation: 8
sem_policy_overlap_metric: dice
# Timestamp-based hold (plan P1.2). sem_policy_hold_frames is retained only as a
# fallback for configs that predate the switch; hold_s wins when > 0.
sem_policy_hold_frames: 180
sem_policy_assist_hold_s: 1.5
sem_policy_strong_hold_s: 1.0
sem_policy_min_state_dwell_s: 0.3

# ------------------------------------------- backend residual weighting w_i ---
# Off for B0 and for U (union without weighting). Owned by `union_weight` and
# `full_adaptive`. U vs U+W differ in sem_geodf_backend_weight ALONE.
sem_geodf_backend_weight: 0
sem_geodf_backend_min_weight: 0.25
sem_geodf_backend_semantic_weight: 0.55
sem_geodf_backend_geo_weight: 0.75
sem_geodf_backend_agree_weight: 0.25
sem_geodf_backend_recovery: 0.20
sem_geodf_rank_by_risk: 1

# ------------------------------------------------------- geometric expert ---
# Off for B0. Owned by the `geodf` / `union*` / `full_adaptive` overlays.
geodf_enable: 0
geodf_hard_reject: 0
geodf_ransac_th_px: 1.0
geodf_sampson_th: 3.0
geodf_min_track_cnt: 2
geodf_min_feature_num: 40
geodf_reject_ratio_max: 0.40
geodf_ratio_guard: 1
geodf_debug: 1
geodf_dump_features: 0
geodf_adaptive: 1
geodf_activate_ratio: 0.12
geodf_activate_ema: 0.15
geodf_deactivate_frac: 0.6
geodf_auto_rho: 1
geodf_auto_mult: 1.8
geodf_auto_margin: 0.05
geodf_activate_ratio_min: 0.10
geodf_activate_ratio_max: 0.40
geodf_auto_floor_down: 0.02
geodf_auto_floor_up: 0.004
geodf_vote_frames: 2
geodf_warmup_frames: 30
# Kept off until the shared stereo validity contract (plan P1.6) gates it.
geodf_stereo_check: 0
geodf_stereo_sampson_th: 6.0
geodf_stereo_floor_max: 0.045
# Degeneracy guards (plan P1.7): GeoDF must not hard-reject on a degenerate F.
geodf_min_grid_occupancy: 0.35
geodf_min_median_parallax_px: 1.0
geodf_max_design_condition_number: 1.0e6
geodf_min_ransac_inliers: 20
geodf_min_ransac_inlier_ratio: 0.35

# ------------------------------------------- stereo physical validity (P1.5) ---
# Shared contract: the same validated stereo matches feed the backend factors and
# the GeoDF right-camera branch. Identical for every method.
stereo_validity_enable: 1
stereo_lr_cycle_max_px: 1.0
stereo_epipolar_max_px: 2.0
stereo_min_disparity_px: 0.5
stereo_max_disparity_px: 200.0
stereo_reprojection_max_px: 2.0
stereo_require_positive_depth: 1

# ------------------------------------------------------------------ backend ---
max_solver_time: 0.04
max_num_iterations: 8
visual_sigma_px: 1.5
visual_huber_delta: 1.0
keyframe_parallax: 10.0

# Common visual/IMU factor adaptation. OFF for B0/G/S/U/U+W: it is a separate
# contribution and must not be bundled into "ours". Only `full_adaptive` turns it
# on, and the plan's second matrix turns it on for ALL methods at once.
visual_adaptive_quality: 0
visual_quality_min_weight: 0.35
visual_lk_error_scale: 20.0
visual_fb_error_scale: 0.5
visual_quality_full_age: 4
visual_adaptive_huber: 0
visual_huber_delta_min: 0.75
visual_huber_delta_max: 3.0
visual_huber_k: 1.345
visual_huber_ema: 0.10
visual_huber_min_samples: 30

acc_n: 0.1
gyr_n: 0.01
acc_w: 0.001
gyr_w: 0.0001
imu_adaptive_covariance: 0
imu_gap_threshold_s: 0.015
imu_acc_saturation: 80.0
imu_gyr_saturation: 8.0
imu_gap_inflation_gain: 3.0
imu_saturation_inflation_gain: 10.0
imu_max_cov_inflation: 20.0
g_norm: 9.81007

# ------------------------------------------------------------- failure/calib ---
# Failure detection is ON for every method (plan P0.4): a diverged run must be
# reported as failed, not silently produce a plausible-looking trajectory.
failure_detection_enable: 1
failure_max_acc_bias: 2.5
failure_max_gyro_bias: 1.0
failure_max_translation_step_m: 5.0
failure_max_rotation_step_deg: 50.0
failure_min_tracked_features: 10
failure_max_consecutive_low_feature_frames: 5

calibration_observability_gate: 1
calibration_min_speed: 0.20
calibration_min_parallax_px: 5.0
calibration_min_tracked_features: 40
estimate_td: 0
td: 0.0

load_previous_pose_graph: 0
pose_graph_save_path: "~/output/pose_graph/"
save_image: 1
