# Reference only (ROS 1 / catkin)

This tree is the upstream VINS-Fusion ROS 1 layout kept for comparison. It is **not built** by colcon (`COLCON_IGNORE`).

Active ROS 2 code lives in the sibling packages under `src/pht_*`.
